from ultralytics import YOLO
import os

# 1. ระบุตำแหน่งไฟล์โมเดล (ใช้ Path เดิมที่หาเจอเมื่อกี้)
MODEL_PATH = r'C:\Users\hemux\OneDrive\Desktop\Newsubmarin\underwater-project-v1\ai_service_Dctect\runs\detect\predict3\fish_video.avi'

# 2. ระบุตำแหน่งไฟล์วิดีโอที่จะทดสอบ
# *** ให้คุณเอารูปแบบไฟล์วิดีโอ (.mp4) มาวางในโฟลเดอร์เดียวกับไฟล์นี้ แล้วแก้ชื่อตรงนี้ครับ ***
VIDEO_PATH = 'fish_video.mp4' 

def main():
    # เช็คว่าไฟล์โมเดลอยู่จริงไหม
    if not os.path.exists(MODEL_PATH):
        print(f"❌ ไม่เจอไฟล์โมเดลที่: {MODEL_PATH}")
        return

    # เช็คว่าไฟล์วิดีโออยู่จริงไหม
    if not os.path.exists(VIDEO_PATH):
        print(f"❌ ไม่เจอไฟล์วิดีโอชื่อ: {VIDEO_PATH}")
        print("💡 อย่าลืมเอาไฟล์วิดีโอมาวางไว้ในโฟลเดอร์นี้ หรือแก้ตัวแปร VIDEO_PATH ให้ถูกนะครับ")
        return

    print(f"🔮 กำลังโหลดโมเดล... เตรียมรันกับวิดีโอ")
    model = YOLO(MODEL_PATH)

    # 3. สั่งรันกับวิดีโอ (Inference)
    # show=True  : เปิดหน้าต่างโชว์ตอนรันเลย (จะได้เห็นสดๆ)
    # save=True  : บันทึกวิดีโอผลลัพธ์เก็บไว้ดูทีหลัง
    # conf=0.4   : เอาความมั่นใจ 40% ขึ้นไป (ปรับลดลงได้ถ้าวิดีโอขุ่นมาก)
    results = model.predict(source=VIDEO_PATH, show=True, save=True, conf=0.4)

    print("\n✅ เรียบร้อย! วิดีโอผลลัพธ์ถูกบันทึกไว้ที่โฟลเดอร์: runs/detect/predict")

if __name__ == '__main__':
    main()